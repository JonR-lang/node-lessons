const stuff = require('./module');
const names = ['Johnny', 'Clinton', 'Ozioma', 'Chichebe', 'Bash', 'Maxwell'];
const numbers = [20,30, stuff.pi]
console.log(stuff.mycounter(names))
console.log(stuff.arrayAdder(numbers))